# Configuração do Servidor
server.port=8080

# Configuração do Banco de Dados H2 (Em memória para facilidade de execução)
spring.datasource.url=jdbc:h2:mem:logitrackdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configuração JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configuração de Segurança (Desabilitando temporariamente para facilitar o desenvolvimento da API)
# spring.security.user.name=user
# spring.security.user.password=password
